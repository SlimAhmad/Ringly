namespace Ringly.Samples.WebApi.Models.Foundations.TelephonyIdentities;

// Rider/Driver — this sample's domain is ride-hailing (matches docker/asterisk's own
// "ride_hailing" dialplan context/ARI app used throughout the rest of this sample).
public enum TelephonyIdentityType
{
    Rider,
    Driver
}
