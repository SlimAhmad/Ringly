[modules]
autoload = yes

; PJSIP replaces legacy chan_sip — avoid loading both.
noload = chan_sip.so

; Unused local audio/console interfaces (headless container).
noload = chan_alsa.so
noload = chan_console.so
noload = chan_oss.so
