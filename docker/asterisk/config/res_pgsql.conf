;
; §6 item 8 — native PostgreSQL realtime connection (res_config_pgsql), so
; row #7's dynamic config PUT (InsertSipEndpointConfigAsync) actually persists.
;
[general]
hostname = postgres
dbname = asterisk
user = asterisk
password = asterisk
port = 5432
requirements = warn
